-- Deletar da tabela estados
delete from estados
-- Que tenha a sigla NV
where sigla = 'NV'

-- Deletar da tabela estados
DELETE FROM estados
-- So se o id for igual ou maior que mil
WHERE id >= 1000 

select * from estados
